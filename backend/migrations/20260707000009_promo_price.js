// Акции/скидки: акционная цена позиции, задаётся админом на любое блюдо.
// null — акции нет. Хранится именно ЦЕНА (а не процент): при синке из POS
// базовая цена может измениться, акционная остаётся какой её задал админ,
// процент для бейджа «-X%» вычисляется на лету. Если акционная цена стала
// >= базовой (например, POS уценил позицию ниже акции) — скидка просто не
// показывается и не применяется. Синк меню эту колонку не трогает.
export async function up(knex) {
  await knex.schema.alterTable('products', (t) => {
    t.decimal('promo_price', 10, 2);
  });
}

export async function down(knex) {
  await knex.schema.alterTable('products', (t) => {
    t.dropColumn('promo_price');
  });
}
